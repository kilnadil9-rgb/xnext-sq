import MapScreen from '../../components/map/MapScreen'

/**
 * /dashboard/map — interactive quest discovery map.
 * Thin page wrapper; all logic lives in components/map.
 */
export function MapPage() {
  return <MapScreen />
}

export default MapPage
